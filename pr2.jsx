function pr2(props) {
    return (
    <h1>{props.name}: {props.price+5}</h1>    //concaate thase add nai thay  &&jo pr1 na price ne {} ma lakhyo hase to add thase
    );
}
export default pr2;


//write a pprogram using react js in which you have to print name of student and university which are past as procs using json.